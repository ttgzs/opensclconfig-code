OpenSASConfigurator Notes and News

* Installation

Currently you need to copy this application and its library (DLLs) in the folder
where it is executed from.

* Compilation

On Win64 you need to set target CPU to 32 bits.
